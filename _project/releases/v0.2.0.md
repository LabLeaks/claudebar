# v0.2.0 — Session Intelligence (provisional)

## Theme

Make session management smarter. claudebar should always resume the right session and offer sensible choices when ambiguity exists.

## Planned

- [x] Differentiate claudebar sessions from raw Claude sessions on resume
- [x] Startup picker for adopting orphan raw Claude sessions
- [x] Don't auto-resume killed claudebar sessions
- [x] Cross-reference state files against live tmux sessions (stale cleanup)
- [ ] Show unclaimed sessions alongside live tmux sessions in the multi-session picker
- [ ] `claudebar gc` — clean up stale state files
- [ ] Test `claude --resume <id>` with extra flags; allow extra args on resume if it works

## Code quality

- [ ] Unify timeAgo and timeAgoShort
- [ ] Cache loadTeamConfig in agent view (called every 1s tick)
- [ ] Fix wrapText newline preservation

## Status

In progress. Core session differentiation landed. Remaining items are polish.
